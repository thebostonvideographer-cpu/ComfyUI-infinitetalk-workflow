# Example Assets

This public repo does not include binary sample media.

Provide your own local source files when you run the setup helper:

- Character PNG with transparent or solid background
- PCM WAV dialogue clip

The setup helper copies the selected image and audio into your ComfyUI input directory and writes a patched workflow to `outputs/FatAndy_SINGLE_SPEAKER.json`.
